(()=>{'use strict';const $=s=>document.querySelector(s),$$=s=>[...document.querySelectorAll(s)];
const state={driver:0,car:0,route:0,racing:false,started:false,paused:false,lane:0,speed:0,boost:100,progress:0,time:0,checkpoint:0,passes:0,keys:{},traffic:[],hit:0};
const driveCanvas=$('#driveCanvas'),driveCtx=driveCanvas.getContext('2d'),trafficArt=new Image();trafficArt.src='./art2d/volt-gt.png';let roadPhase=0;
function sizeDriveCanvas(){let d=Math.min(devicePixelRatio||1,2),r=driveCanvas.getBoundingClientRect();driveCanvas.width=Math.max(1,Math.round(r.width*d));driveCanvas.height=Math.max(1,Math.round(r.height*d));driveCtx.setTransform(d,0,0,d,0,0)}addEventListener('resize',sizeDriveCanvas);sizeDriveCanvas();
function renderDrive(dt){let w=driveCanvas.clientWidth,h=driveCanvas.clientHeight,c=driveCtx;if(!w||!h)return;c.clearRect(0,0,w,h);if(!state.racing||!state.started||state.paused)return;roadPhase=(roadPhase+state.speed*dt/370)%1;let hy=h*.35,curve=Math.sin(state.progress*9)*w*.06,center=w*.5+curve,bottom=w*.64,top=w*.14;c.save();c.fillStyle='rgba(5,8,20,.82)';c.beginPath();c.moveTo(center-top,hy);c.lineTo(center+top,hy);c.lineTo(center+bottom,h);c.lineTo(center-bottom,h);c.closePath();c.fill();c.strokeStyle='rgba(91,238,255,.46)';c.lineWidth=3;c.beginPath();c.moveTo(center-top,hy);c.lineTo(center-bottom,h);c.moveTo(center+top,hy);c.lineTo(center+bottom,h);c.stroke();for(let stripe=0;stripe<22;stripe++){let t=(stripe/22+roadPhase)%1,t2=Math.min(1,t+.038),y=hy+(h-hy)*t*t,y2=hy+(h-hy)*t2*t2,half=top+(bottom-top)*t*t,half2=top+(bottom-top)*t2*t2;for(let lane=-1;lane<=1;lane+=2){let x1=center+lane*half/3,x2=center+lane*half2/3,mark=Math.max(1,2+t*8);c.fillStyle=`rgba(255,234,156,${.28+t*.7})`;c.beginPath();c.moveTo(x1-mark,y);c.lineTo(x1+mark,y);c.lineTo(x2+mark*1.8,y2);c.lineTo(x2-mark*1.8,y2);c.closePath();c.fill()}}for(let i=0;i<10;i++){let t=(i/10+roadPhase*.58)%1,y=hy+(h-hy)*t*t,edge=top+(bottom-top)*t*t;c.fillStyle=`rgba(255,173,80,${.18+t*.7})`;c.beginPath();c.arc(center-edge-17*t,y,2+t*7,0,Math.PI*2);c.arc(center+edge+17*t,y,2+t*7,0,Math.PI*2);c.fill()}for(let car of state.traffic){let t=car.z,y=hy+(h-hy)*t*t,half=top+(bottom-top)*t*t,x=center+car.lane*half*.53,stateSize=42+t*210;if(trafficArt.complete){c.save();c.filter=`hue-rotate(${car.hue}deg) saturate(1.35) drop-shadow(0 10px 8px rgba(0,0,0,.8))`;c.drawImage(trafficArt,x-stateSize*.5,y-stateSize*.31,stateSize,stateSize*.62);c.restore()}else{c.fillStyle=car.color;c.fillRect(x-stateSize*.35,y-stateSize*.17,stateSize*.7,stateSize*.34)}}c.restore()}
const drivers=[['IZZY MONROE','IM','CURRENT RUNNER','ROUTE CURRENT','Finds the clearest way through the blackout.'],['AALIYAH GAIN','AG','VELOCITY','ACCELERATE','Turns every open street into an advantage.'],['THEO REED','TR','POWER LINE','BREAKAWAY','Keeps the rescue run moving forward.'],['JORDAN KIM','JK','SIGNAL TECH','OVERRIDE','Reads the city faster than it can close in.'],['MATEO RIVERA','MR','STREETWISE','SHORTCUT','Knows which streets still lead home.']];
const cars=[['VOLT GT','#50e7ed',184,'Twin-turbo street coupé'],['NIGHTFALL R','#a47aff',176,'Low-slung grand tourer'],['SUNFIRE ZX','#ff5b9e',191,'Lightweight sport compact'],['RIVET 4X','#ffbd4d',171,'All-weather rally machine']];
const routes=[['LAKEFRONT ESCAPE','Aldermoor → Lakefront Safe Zone','lakefront','Two laps · wet boulevard · marina lights','./art2d/lakefront.png'],['CASINO CAUSEWAY','Founders Square → Salk Bingo and Casino','casino','Two laps · tunnel sweep · casino express','./art2d/casino.png'],['LAMBEAU NIGHT RUN','Lambeau High → Clearview','lambeau','Two laps · stadium hill · lake curve','./art2d/lambeau.png']];
function selectUI(){
 $('#drivers').innerHTML=drivers.map((d,i)=>`<button class="pick ${i===state.driver?'selected':''}" data-driver="${i}"><span class="tag">${d[2]}</span><span class="initial">${d[1]}</span><h3>${d[0]}</h3><p>${d[4]}</p><small class="ability">⚡ ${d[3]}</small></button>`).join('');
 $('#cars').innerHTML=cars.map((c,i)=>`<button class="pick car-pick ${i===state.car?'selected':''}" data-car="${i}"><img src="./art2d/volt-gt.png" alt=""><span class="tag">ORIGINAL 1999 SPORTS CAR</span><h3>${c[0]}</h3><p>${c[3]}</p><div class="stat">TOP ${c[2]} MPH<br>GRIP ${'▮'.repeat(3+i%2)}</div></button>`).join('');
 $('#routeCards').innerHTML=routes.map((r,i)=>`<button class="route-card ${i===state.route?'selected':''}" data-route="${i}" style="--route-img:url('${r[4]}')"><div><span class="tag">${r[3].toUpperCase()}</span><h3>${r[0]}</h3><p>${r[1]}</p><small>12 CHECKPOINTS · PASSENGER ABOARD FROM START</small></div></button>`).join('');
 $$('[data-driver]').forEach(b=>b.onclick=()=>{state.driver=+b.dataset.driver;selectUI()});$$('[data-car]').forEach(b=>b.onclick=()=>{state.car=+b.dataset.car;selectUI()});$$('[data-route]').forEach(b=>b.onclick=()=>{state.route=+b.dataset.route;selectUI()});
}selectUI();
function show(id){$$('.screen').forEach(x=>x.classList.remove('active'));if(id!=='none')$('#'+id).classList.add('active')}
window.sparkedShow=show;
document.addEventListener('click',event=>{let button=event.target.closest('[data-screen]');if(button){event.preventDefault();show(button.dataset.screen)}});
function setRoute(){let r=routes[state.route],app=$('#app');app.className='route-'+r[2];$('#routeName').textContent=r[0];$('#playerCar').style.filter=`hue-rotate(${state.car*68}deg) saturate(1.25) drop-shadow(0 28px 22px rgba(0,0,0,.65))`;}
function spawnTraffic(){state.traffic=[];$('#traffic').innerHTML='';for(let i=0;i<3;i++)state.traffic.push({lane:[-1,1,0][i],z:.12+i*.24,speed:.68+i*.07,passed:false,hue:[55,290,125][i],color:['#ffb14f','#ff54a5','#65f5e3'][i]})}
function launch(){Object.assign(state,{racing:true,started:false,paused:false,lane:0,speed:0,boost:100,progress:0,time:0,checkpoint:0,passes:0,hit:0});roadPhase=0;setRoute();spawnTraffic();$('#hud').hidden=false;$('#boostHud').hidden=false;$('#touch').hidden=innerWidth>720;show('none');countdown(3)}
function countdown(n){let e=$('#countdown');e.textContent=n||'GO!';e.classList.add('show');setTimeout(()=>{if(n)countdown(n-1);else{e.classList.remove('show');state.started=true}},700)}
function updateTraffic(dt){for(let t of state.traffic){t.z+=dt*t.speed*(state.speed/150)*.18;if(t.z>1.06){t.z=.06+Math.random()*.08;t.lane=[-1,0,1][Math.floor(Math.random()*3)];t.speed=.65+Math.random()*.18;t.passed=false}if(t.z>.88&&!t.passed){t.passed=true;state.passes++;state.boost=Math.min(100,state.boost+8)}if(t.z>.56&&t.z<.76&&Math.abs(t.lane-state.lane)<.27&&state.hit<=0){state.hit=.75;state.speed*=.63;state.boost=Math.max(0,state.boost-15);$('#raceStage').animate([{transform:'translateX(-5px)'},{transform:'translateX(5px)'},{transform:'translateX(0)'}],{duration:190})}}}
function updateHUD(){let check=Math.min(12,Math.floor(state.progress*12)+1);$('#progress').style.width=(state.progress*100)+'%';$('#checkpoint').textContent=String(check).padStart(2,'0')+' / 12';$('#speed').innerHTML=String(Math.round(state.speed)).padStart(3,'0')+' <em>MPH</em>';$('#boostValue').textContent=Math.round(state.boost)+'%';$('#boostBar').style.width=state.boost+'%';$('#position').textContent=String(Math.max(1,4-Math.floor(state.passes/4))).padStart(2,'0')+' / 04'}
function update(dt){if(!state.racing||!state.started||state.paused)return;let boost=(state.keys.boost||state.keys[' '])&&state.boost>1,brake=state.keys.brake||state.keys.down||state.keys.s,target=brake?58:cars[state.car][2]*(boost?1.22:1);state.speed+=(target-state.speed)*dt*(brake?.8:1.15);state.speed=Math.max(25,state.speed);let steer=(state.keys.left||state.keys.a?-1:0)+(state.keys.right||state.keys.d?1:0);state.lane=Math.max(-1.12,Math.min(1.12,state.lane+steer*dt*1.45));if(boost)state.boost=Math.max(0,state.boost-dt*27);else state.boost=Math.min(100,state.boost+dt*8);state.progress+=state.speed*dt/16000;state.time+=dt;state.hit=Math.max(0,state.hit-dt);let app=$('#app');app.style.setProperty('--drift',(-state.lane*42)+'px');app.style.setProperty('--car-x',(state.lane*210)+'px');app.classList.toggle('boosting',boost);app.classList.toggle('turn-left',steer<0);app.classList.toggle('turn-right',steer>0);updateTraffic(dt);if(state.progress>=1)finish();updateHUD()}
function finish(){state.racing=false;state.started=false;$('#hud').hidden=true;$('#boostHud').hidden=true;$('#touch').hidden=true;let n=drivers[state.driver][0].split(' ')[0],time=`${Math.floor(state.time/60)}:${String(Math.floor(state.time%60)).padStart(2,'0')}`;$('#finishCopy').innerHTML=`<p>SAFE ZONE REACHED</p><h2>YOU GOT THEM HOME.</h2><span>${n} carried the passenger through ${routes[state.route][0]} in ${time}. The race never stopped.</span><div class="result-stats">12 / 12 CHECKPOINTS · ${state.passes} CLEAN OVERTAKES · ${Math.round(state.boost)}% SPARK REMAINING</div><button class="primary" id="again">RUN ANOTHER ROUTE <b>→</b></button>`;show('finish');$('#again').onclick=()=>show('routes')}
let last=performance.now();function loop(now){let dt=Math.min(.05,(now-last)/1000);last=now;update(dt);renderDrive(dt);requestAnimationFrame(loop)}requestAnimationFrame(loop);
$('#launch').onclick=launch;$('#resume').onclick=()=>{state.paused=false;show('none')};$('#restart').onclick=launch;$('#quit').onclick=()=>{state.racing=false;state.started=false;$('#hud').hidden=true;$('#boostHud').hidden=true;show('garage')};
function key(e){return({arrowleft:'left',arrowright:'right',arrowup:'up',arrowdown:'down'}[e.key.toLowerCase()]||e.key.toLowerCase())}addEventListener('keydown',e=>{if(['ArrowLeft','ArrowRight','ArrowUp','ArrowDown',' ','Escape'].includes(e.key))e.preventDefault();if(e.key==='Escape'&&state.racing){state.paused=!state.paused;show(state.paused?'pause':'none');return}let k=key(e);state.keys[k]=true;if(k==='shift')state.keys.boost=true;if(e.key===' ')state.keys[' ']=true});addEventListener('keyup',e=>{let k=key(e);state.keys[k]=false;if(k==='shift')state.keys.boost=false;if(e.key===' ')state.keys[' ']=false});$$('#touch button').forEach(b=>{let k=b.dataset.key,down=e=>{e.preventDefault();state.keys[k]=true},up=e=>{e.preventDefault();state.keys[k]=false};b.addEventListener('pointerdown',down);b.addEventListener('pointerup',up);b.addEventListener('pointerleave',up);b.addEventListener('pointercancel',up)});
})();
